/** Independent Arm Override command and rover-reported state values. */
export enum ArmOverrideCommand {
  Enable = 'ENABLE',
  Disable = 'DISABLE',
}

export enum ArmOverrideState {
  Inactive = 'INACTIVE',
  Active = 'ACTIVE',
}
