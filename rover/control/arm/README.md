# arm code
to control an arm

Proposed architecture:

( Joystick -> Arm controller -> GUI ) >> ( GUI -> Driver -> Can brdige -> Motors )
              Base station                                Rover
